# Issue 标签规范

## 维度
- module: `module:runtime|modeling|data|api|frontend|devops`
- priority: `priority:P1|P2|P3`
- type: `type:bug|feat|task|doc`
- status: `status:blocked|in-progress|done`

## 使用建议
- 每个 Issue 至少包含 `type` 与 `module`；按里程碑统一 `priority`；`status` 反映当前进展。
