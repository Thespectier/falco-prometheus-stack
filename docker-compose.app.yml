version: '3.8'
services:
  api:
    build: ./api
    container_name: app-api
    profiles: [dev]
    ports:
      - "8000:8000"
    command: ["sleep", "infinity"]
  web:
    build: ./web
    container_name: app-web
    profiles: [dev]
    ports:
      - "5173:5173"
    command: ["sleep", "infinity"]
