FROM python:3.11-alpine

WORKDIR /app

# Install postgresql-client for CLI fallback support
RUN apk add --no-cache postgresql-client

RUN pip install psycopg2-binary

# Copy the test script
COPY test/ test/

ENV PYTHONPATH=/app

CMD ["python", "test/query_alerts_db.py"]
